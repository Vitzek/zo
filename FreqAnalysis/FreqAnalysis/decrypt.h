#pragma once
#include <iostream>
#include <string>
#include <fstream>
using namespace std;

void decryptMono(string, string, string, string);

int* monoSort(int*);

int* read64(string, int*);

void replace(int*, int*, string, string);

void decryptBi(string, string, string, string, string, string);

